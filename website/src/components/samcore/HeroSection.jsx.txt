import React from 'react';
import { motion } from 'framer-motion';
import { ArrowRight, Truck, Clock, ShieldCheck } from 'lucide-react';
import { Button } from '@/components/ui/button';

export default function HeroSection({ heroImage }) {
  return (
    <section className="relative min-h-screen flex items-center overflow-hidden">
      {/* Background Image */}
      <div className="absolute inset-0">
        <img
          src={heroImage}
          alt="Samcore bulk water tanker at dusk on Alberta highway"
          className="w-full h-full object-cover"
        />
        <div className="absolute inset-0 bg-gradient-to-r from-background via-background/85 to-background/40" />
        <div className="absolute inset-0 bg-gradient-to-t from-background via-transparent to-background/30" />
      </div>

      {/* Decorative Grid Lines */}
      <div className="absolute inset-0 opacity-5">
        <div className="h-full w-full" style={{
          backgroundImage: 'linear-gradient(to right, hsl(var(--foreground)) 1px, transparent 1px)',
          backgroundSize: '120px 100%'
        }} />
      </div>

      {/* Decorative Coordinates */}
      <div className="absolute top-32 right-8 hidden xl:block">
        <p className="font-mono text-xs text-muted-foreground/30 tracking-widest">53.5461° N</p>
        <p className="font-mono text-xs text-muted-foreground/30 tracking-widest">113.4937° W</p>
        <p className="font-mono text-xs text-muted-foreground/30 tracking-widest mt-2">EDMONTON, AB</p>
      </div>

      <div className="relative z-10 max-w-7xl mx-auto px-6 lg:px-8 pt-32 pb-24">
        <motion.div
          initial={{ opacity: 0, y: 40 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: [0.22, 1, 0.36, 1] }}
          className="max-w-3xl"
        >
          <div className="flex items-center gap-3 mb-8">
            <div className="h-px w-12 bg-primary" />
            <span className="text-primary font-heading font-semibold text-sm tracking-[0.25em] uppercase">
              Edmonton, Alberta
            </span>
          </div>

          <h1 className="font-heading font-extrabold tracking-tight leading-none mb-2">
            <span className="block text-foreground text-5xl sm:text-6xl md:text-7xl lg:text-8xl">
              BULK WATER
            </span>
            <span className="block text-transparent text-5xl sm:text-6xl md:text-7xl lg:text-8xl mt-1" style={{
              WebkitTextStroke: '2px hsl(var(--foreground))'
            }}>
              HAULING
            </span>
          </h1>

          <p className="text-lg md:text-xl text-muted-foreground font-body leading-relaxed mt-8 max-w-xl">
            Professional bulk water transportation services for residential, commercial,
            construction and industrial operations across Alberta.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 mt-10">
            <a href="#contact">
              <Button size="lg" className="bg-primary hover:bg-primary/90 text-primary-foreground font-heading font-semibold tracking-wide text-base px-8 h-14 group">
                Get a Free Quote
                <ArrowRight className="ml-2 w-5 h-5 group-hover:translate-x-1 transition-transform" />
              </Button>
            </a>
            <a href="#services">
              <Button size="lg" variant="outline" className="border-2 border-foreground/20 hover:border-foreground/40 text-foreground font-heading font-semibold tracking-wide text-base px-8 h-14">
                Our Services
              </Button>
            </a>
          </div>
        </motion.div>
      </div>

      {/* Bottom Ticker */}
      <div className="absolute bottom-0 left-0 right-0 border-t border-border bg-background/80 backdrop-blur-sm">
        <div className="max-w-7xl mx-auto px-6 lg:px-8">
          <div className="grid grid-cols-1 sm:grid-cols-3 divide-y sm:divide-y-0 sm:divide-x divide-border">
            <div className="flex items-center gap-3 py-4 px-2">
              <Truck className="w-5 h-5 text-primary flex-shrink-0" />
              <span className="text-sm text-muted-foreground font-medium">Fleet Ready for Dispatch</span>
            </div>
            <div className="flex items-center gap-3 py-4 px-2">
              <Clock className="w-5 h-5 text-primary flex-shrink-0" />
              <span className="text-sm text-muted-foreground font-medium">24/7 Emergency Service Available</span>
            </div>
            <div className="flex items-center gap-3 py-4 px-2">
              <ShieldCheck className="w-5 h-5 text-primary flex-shrink-0" />
              <span className="text-sm text-muted-foreground font-medium">Fully Licensed & Insured</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}